const H1 = ({ children }) => {
    return (
        <h1 className="">{ children }</h1>
    );
};

const H2 = ({ children }) => {
    return (
        <h2 className="">{ children }</h2>
    );
};

const H3 = ({ children }) => {
    return (
        <h3 className="">{ children }</h3>
    );
};

export {
    H1,
    H2,
    H3
}